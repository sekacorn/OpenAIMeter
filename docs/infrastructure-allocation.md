# Infrastructure Allocation

Infrastructure allocation supports weighted Decimal allocation and preserves rounded totals. Built-in allocation methods include equal share, request count, duration, token count, successful outcome, workflow weight, and custom supplied weights.
