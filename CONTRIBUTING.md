# Contributing

Use `sekacorn` as the public project author identity. Keep core dependencies minimal and optional integrations optional. Run Ruff, mypy, pytest with branch coverage, Bandit, build, and Twine check before release preparation.
